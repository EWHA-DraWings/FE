import 'package:flutter/material.dart';

class PastReport extends StatelessWidget {
  const PastReport({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: const BoxDecoration(
        color: Colors.red,
      ),
    );
  }
}
